"use client";
import {ChangeEvent,useCallback,useEffect,useMemo,useRef,useState} from "react";
import BevMap,{SceneObject,SceneState} from "./BevMap";
import BuildingSurfaceEditor from "./BuildingSurfaceEditor";
import CarPreview,{MeshData} from "./CarPreview";
import PedestrianClothingEditor from "./PedestrianClothingEditor";
import ProjectionEditor from "./ProjectionEditor";
import RoadSurfaceEditor from "./RoadSurfaceEditor";

type Studio="vehicle"|"road"|"building"|"pedestrian";
const bridge="http://127.0.0.1:8765",labels:Record<Studio,string>={vehicle:"Vehicle",road:"Road",building:"Building",pedestrian:"Pedestrian"};
export default function Home(){
 const output=useRef<HTMLCanvasElement>(null);const [studio,setStudio]=useState<Studio>("vehicle"),[image,setImage]=useState<HTMLImageElement|null>(null),[base,setBase]=useState<HTMLImageElement|null>(null),[mesh,setMesh]=useState<MeshData|null>(null),[fileName,setFileName]=useState("No image selected"),[revision,setRevision]=useState(0),[applyState,setApplyState]=useState("Bridge ready"),[applying,setApplying]=useState(false),[scene,setScene]=useState<SceneState|null>(null),[selected,setSelected]=useState("");
 useEffect(()=>{const n=new Image();n.onload=()=>setBase(n);n.src="/tesla-model3-bodywork.png";fetch("/tesla-model3-uv0.json").then(r=>r.json()).then(setMesh)},[]);
 useEffect(()=>{let live=true;const poll=()=>fetch(`${bridge}/scene/state`).then(r=>r.json()).then(v=>{if(live)setScene(v)}).catch(()=>{});poll();const timer=setInterval(poll,700);return()=>{live=false;clearInterval(timer)}},[]);
 const rendered=useCallback(()=>setRevision(v=>v+1),[]),objects=useMemo(()=>{if(!scene)return[];const all=[...scene.static_targets,...scene.objects];return all.filter(o=>studio==="pedestrian"?o.kind==="walker":o.kind===studio)},[scene,studio]);
 useEffect(()=>{if(objects.length&&!objects.some(o=>o.key===selected))setSelected(objects[0].key)},[objects,selected]);
 function upload(e:ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f)return;const u=URL.createObjectURL(f),n=new Image();n.onload=()=>{setImage(n);URL.revokeObjectURL(u)};n.src=u;setFileName(f.name)}
 function png(){const a=document.createElement("a");a.download=`${studio}-texture.png`;a.href=output.current?.toDataURL("image/png")??"";a.click()}
 async function post(path:string,data:unknown){const r=await fetch(`${bridge}/${path}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)}),v=await r.json();if(!r.ok)throw new Error(v.error);return v}
 async function focus(o:SceneObject){const compatible=studio==="pedestrian"?o.kind==="walker":o.kind===studio;if(compatible)setSelected(o.key);setApplyState(`Focused ${o.label}`);if(o.id)await post("focus/actor",{actor_id:o.id});else await post("focus/static",{kind:o.kind,name:o.name})}
 async function toggleHero(){try{const v=await post("camera/hero-lock",{enabled:!scene?.hero_lock});setApplyState(v.enabled?`Hero camera locked to actor ${v.hero_id}`:"Hero camera released")}catch(e){setApplyState(e instanceof Error?e.message:"Hero camera failed")}}
 async function apply(){if(!output.current)return;setApplying(true);setApplyState("Uploading texture…");try{const blob=await new Promise<Blob>((ok,no)=>output.current!.toBlob(v=>v?ok(v):no(new Error("PNG encoding failed")),"image/png"));const target=objects.find(o=>o.key===selected),endpoint=studio==="vehicle"?"apply":studio==="road"?`apply/road?scope=${encodeURIComponent(target?.name==="Town04 roads"?"all":target?.name??"all")}`:studio==="building"?`apply/building?target=${encodeURIComponent(target?.name??"BP_House16")}`:"apply/pedestrian";const r=await fetch(`${bridge}/${endpoint}`,{method:"POST",headers:{"Content-Type":"image/png"},body:blob}),v=await r.json();if(!r.ok)throw new Error(v.error);setApplyState(`Applied ${studio} texture to ${v.object_name??v.object_count??v.actor_id}`)}catch(e){setApplyState(e instanceof Error?e.message:"Apply failed")}finally{setApplying(false)}}
 function changeStudio(next:Studio){setStudio(next);setImage(null);setFileName("No image selected");setApplyState("Bridge ready")}
 const target=objects.find(o=>o.key===selected),title=studio==="vehicle"?"TESLA BODY SURFACE":studio==="road"?"ROAD SURFACE":studio==="building"?"BUILDING WALL SURFACE":"PEDESTRIAN CLOTHING";
 return <main><header><div><span className="eyebrow">CARLA SAFETY AGENT</span><h1>Scene Control Studio</h1></div><div className="status"><i/>{scene?`${scene.map.split("/").pop()} · ${scene.objects.length} dynamic objects`:"Connecting to CARLA…"}</div></header><div className="viewtabs">{(Object.keys(labels) as Studio[]).map(v=><button key={v} className={studio===v?"active":""} onClick={()=>changeStudio(v)}>{labels[v]}</button>)}</div><section className="workspace"><aside className="controls"><div className="section"><label htmlFor="target">Scene object</label><select id="target" value={selected} onChange={e=>{const o=objects.find(v=>v.key===e.target.value);if(o)focus(o)}}>{objects.length?objects.map(o=><option value={o.key} key={o.key}>{o.label}</option>):<option value="">No matching objects</option>}</select><button disabled={!target} onClick={()=>target&&focus(target)}>Focus & highlight</button></div><div className="section"><button className={scene?.hero_lock?"primary":""} onClick={toggleHero}>{scene?.hero_lock?"Release hero camera":"Lock hero camera"}</button><small>Follow the only role_name=hero vehicle from a fixed chase view.</small></div><div className="section"><label className="upload">Upload image<input type="file" accept="image/png,image/jpeg,image/webp" onChange={upload}/></label><small>{fileName}</small></div><div className="note"><strong>Category highlights</strong><p>Vehicles blue · pedestrians yellow · buildings purple · roads red.</p></div></aside><div className="visuals"><div className="stage surface-stage"><div className="stagebar"><span>{title}</span><span>{studio==="vehicle"?"Drag image · wheel to scale":"Local decal or tiled surface"}</span></div>{studio==="vehicle"?<ProjectionEditor mesh={mesh} base={base} image={image} output={output} onRender={rendered}/>:studio==="road"?<RoadSurfaceEditor image={image} output={output} onRender={rendered}/>:studio==="building"?<BuildingSurfaceEditor image={image} output={output} onRender={rendered}/>:<PedestrianClothingEditor image={image} output={output} onRender={rendered}/>}<canvas className="atlas-output" ref={output} width={studio==="vehicle"?2048:1024} height={studio==="vehicle"?2048:1024}/></div>{studio==="vehicle"?<CarPreview mesh={mesh} atlas={output} revision={revision}/>:<div className="preview road-preview"><div className="previewbar"><b>SELECTED TARGET</b><span>{target?.label??"None"}</span></div><div className="road-perspective"><span>Independent materials preserved</span></div></div>}<BevMap state={scene} selected={selected} onPick={focus}/></div><aside className="export"><span className="eyebrow">LIVE OUTPUT</span><h2>Send to CARLA</h2><p>Apply the composed local decal or tile only to the selected verified surface target.</p><button className="primary" disabled={!image||applying||!target} onClick={apply}>{applying?"Applying…":`Apply ${studio} texture`}</button><small>{applyState}</small><button disabled={!image} onClick={png}>Export texture PNG</button><dl><div><dt>Target</dt><dd>{target?.kind??"None"}</dd></div><div><dt>BEV</dt><dd>Live · click to focus</dd></div><div><dt>Hero</dt><dd>{scene?.hero_lock?"Locked":"Free"}</dd></div></dl></aside></section></main>
}
